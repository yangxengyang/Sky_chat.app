# Workspace Rules - SkyChat

## Environment Variables & Security
- Never commit or push `.env`, `.env.local`, or any private credential files to Git repository.
- Always include `.env` in `.gitignore` and only commit `.env.example` as a template reference.
